// types/job.ts
export interface Job {
  id?: string | null;
  employerId?: string | null;
  // (optional) add more fields as you need them later:
  // title?: string | null;
  // description?: string | null;
  // location?: string | null;
  // etc.
}
