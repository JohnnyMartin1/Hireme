# Database
DATABASE_URL="postgresql://postgres:yourpassword@localhost:5432/hireme"

# NextAuth (auth secret + base URL)
NEXTAUTH_SECRET="replace_with_a_random_long_string"
NEXTAUTH_URL="http://localhost:3000"

# Email (Mailtrap dev, switch to SendGrid/Resend in prod)
SMTP_HOST="smtp.mailtrap.io"
SMTP_PORT=587
SMTP_USER="your-mailtrap-username"
SMTP_PASS="your-mailtrap-password"
EMAIL_FROM="HireMe <no-reply@hireme.local>"

# Uploads (local dev folder)
UPLOAD_DIR="./public/uploads"
DATABASE_URL="postgresql://catherinefratila@localhost:5432/hireme"
DEV_EMAIL_LOG=true
NEXTAUTH_URL=http://localhost:3000
